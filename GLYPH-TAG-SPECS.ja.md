# Glyph Tag Specification (仕様書)
**Author:** [jp-rad](https://github.com/jp-rad)  
**Established:** 2026‑10‑02 (つるっつる豆腐の日)  


## 1. 適用範囲および目的（Scope and Purpose）

本仕様書は、文字図形識別名（`glyph-name`）を基軸として字形情報を抽象化・表現するための共通タグ形式「Glyph Tag」の構文および処理モデルを規定します。

### 1.1 背景と目的（Background and Purpose）

デジタル環境における未定義グリフの代替表示（いわゆる「豆腐文字」）の発生、および既存システム等に閉じた特定の字形構造は、文字情報の厳密性と相互運用性を著しく損なう長年の構造的課題です。
また、異体字を含む人名等の検索においては、実装文字コード（IVS等）を正確に入力して検索することが困難であり、検索性の低下を招く要因となっています。

本仕様書は、制定日（2026-10-02）を象徴的な基点とし、文字図形識別名（`glyph-name`）を軸とする抽象化層を確立することを目指します。

これにより、表示基盤に依存する豆腐文字の発生を根絶するとともに、標準・非標準を問わず多様な字形体系を包摂・保全し、いかなる環境においても意図された字形の正確かつ堅牢な描画を実現します。
さらに、基本文字コードを用いた共通展開モデルを提供することで、一般的な日本語入力環境からでも人名等の高度な検索・照合を容易にします。

### 1.2 コンセプト：処理系と人間系の両立

Glyph Tag は、**「処理系（プログラム・表示システム）も人間系（開発者・運用者）も直感的に理解できる」** 構造を目指して設計されています。

* **人間系への可読性**: 構造がシンプルで視認しやすく、ソースコードやテキストデータとして直接読んだ際にも意図（文字図形名、代替コード、実装コード）が直感的に把握できます
* **処理系への明確性**: 正規化（データ確定）と描画（表示展開）の責務を分離し、プログラム側で決定論的にパース・処理できる軽量な構文を提供します

### 1.3 主な機能

1. **抽象レイヤの提供**: 文字図形識別名を「基本文字コード」および「実装文字コード」へ対応付けます
2. **共通フォーマットの標準化**: MJ文字図形名やGJ文字図形名、および独自に定義された固有字形をはじめとする異なる字形体系を統一的に扱います
3. **固有文字体系の保全と吸収**: 固有の表示環境が消失した場合でも、抽象タグとして字形識別情報および縮退表示用情報を保持・継承します
4. **検索性の飛躍的向上**: 描画（基本優先モード）を用いて基本文字コードへ展開した文字列を事前に生成・保持しておくことで、特殊な異体字の入力を要しない一般的な入力環境からの容易な人名・住所等の検索を可能にします
5. **ライフサイクル管理（処理モデルの分離）**: 正規化（Normalization）、描画（Rendering）、および簡略化（Simplification）の各フェーズを明確に分離して処理します

## 2. 用語の定義（Terms and Definitions）

本仕様書において使用する主要な用語の定義は以下の通りです。

* **文字図形識別名（glyph-name）**: 特定の字形体系において文字図形（glyph）を一意に識別するための抽象的な名称（例: MJ文字図形名、GJ文字図形名、固有字形識別名）
* **基本文字コード（Basic Character Code）**: Unicode 標準において異体字選択符号（IVS）等を含まない単一の標準コードポイント（`b` 属性）  
未符号化字形等においては縮退表示用の代替文字を含みます
* **実装文字コード（Implementation Character Code）**: 実装基盤において対象の字形を表示するために採用されるコードポイント並び（`v` 属性）  
Unicode 標準文字のほか、IVS（Ideographic Variation Sequence）や PUP（Private Use Plane：私用面）等の固有コードを含みます
* **字形体系識別子（Glyph System Identifier）**: 適用される字形体系を一意に識別するためのコード（`set` 属性）  
* **表示システム**: Glyph Tag を解釈・変換・表示するアプリケーション、オペレーティングシステム、およびフォント等で構成される処理基盤全般  
* **豆腐文字（Tofu）**: 適用可能なコードポイントが存在しない場合、または該当するグリフが描画できない場合に表示システムが出力する代替記号  

## 3. 構文仕様（Syntax Specification）

Glyph Tag の基本構文を以下に示します。

```text
{<glyph-name> [b=<UCSSeq>] [v=<UCSSeq>] [set=<Identifier>]}
```

### 3.1 構成要素

* **`<glyph-name>`（必須）**: 体系固有の文字図形識別名
* **`b=<UCSSeq>`（任意）**: 基本文字コード
* **`v=<UCSSeq>`（任意）**: 実装文字コード
* **`set=<Identifier>`（任意）**: 字形体系識別子

### 3.2 タグの表現形態（基本タグと属性付きタグ）

Glyph Tag には、属性記述の有無に応じて以下の2つの表現形態が存在します。

* **基本タグ（Basic Tag）**: 属性記述（`b`, `v`, `set`）を持たず、`<glyph-name>` のみで構成される `{<glyph-name>}` 形式の表現  データの直接入力、手動記述、および環境に依存しない抽象的な字形指定に用います
* **属性付きタグ（Attributed Tag）**: `<glyph-name>` に加え、1つ以上の属性記述を包含する `{<glyph-name> b=... v=... set=...}` 形式の表現  
正規化処理によって適用環境の定義値が補完・確定された状態であり、処理系での描画や交換に用います

### 3.3 属性の記述規則

* 属性（`b`, `v`, `set`）の記述順序は任意です
* 各属性値は、正規化処理によって自動的に決定・上書きされます

### 3.4 UCSSeq（UCS コードポイント系列）の表現

`UCSSeq` は、1 つ以上の Unicode コードポイントを半角スペース区切りで並べた系列です。

```text
U+XXXX
U+XXXX U+YYYY
U+XXXX U+YYYY U+ZZZZ
```

### 3.5 エスケープ処理規則（Escape Rules）

Glyph Tag の開始記号 `{` を地の文として記述したい場合は、`{{` とエスケープ表現にします。

* **パース対象外**: `{{` はタグ構文の解析対象から除外され、そのまま保護されます（閉じ記号 `}` のエスケープ規則は定義しません）

#### ライフサイクルにおける挙動:
* **正規化・簡略化時**: 保護された `{{` の形態を維持して出力します
* **描画時**: エスケープが解除され、単一の文字 `{` に変換されます

#### 評価例:

**例 1**: `現在の表記は {{MJ022335} です。`
* 正規化・簡略化結果: `現在の表記は {{MJ022335} です。`
* 描画結果: `現在の表記は {MJ022335} です。`

**例 2（後続に `}` が連続する場合）**: `タグ: {MJ022335}}}`
* 正規化結果: `タグ: {MJ022335 b=U+845B v=U+845B U+E0102 set=mj}}}`
* 描画結果: `タグ: 葛}}`


## 4. 処理モデル（Processing Model）

Glyph Tag の処理は、目的の異なる3つの独立したフェーズ（正規化・描画・簡略化）から構成されます。

### 4.1 正規化処理（Normalization Process）

適用環境の字形体系に従い、タグの属性値を決定・上書きしてデータを確定する処理です。基本タグが入力された場合は属性値が付与され、属性付きタグへと変化します。

1. **照合と上書き**: `<glyph-name>` を適用環境の文字図形表と照合し、既存の属性値の有無にかかわらず `b`, `v`, `set` の各属性値を適用環境の定義値で更新します
2. **結果出力**: 属性値が確定した属性付きタグ（正規化タグ）を出力します

#### 入力と出力結果の例

* **基本タグからの正規化**:

    * 入力: `奈良県葛城市の{MJ022335}です`
    * 出力: `奈良県葛城市の{MJ022335 b=U+845B v=U+845B U+E0102 set=mj}です`


* **既存属性の上書き（再正規化）**:

    * 入力: `{MJ022335 b=U+845B}`
    * 出力: `{MJ022335 b=U+845B v=U+845B U+E0102 set=mj}`



---

### 4.2 描画処理（Rendering Process）

正規化済みの Glyph Tag を、表示対象の Unicode 文字列へ展開する処理です。

1. **コードポイント選択**:
    * **実装優先モード（既定）**: 実装文字コード（`v`） ➔ 基本文字コード（`b`） ➔ 豆腐文字  
    *用途: 画面表示や印刷出力など、意図された正確な字形表現を要求する文脈で利用します*
    * **基本優先モード**: 基本文字コード（`b`） ➔ 豆腐文字  
    *用途: 異体字（IVS等）を直接入力することが困難な一般の日本語入力環境からの検索漏れを防ぐため、事前に平易な標準文字コードのみへ展開した検索用文字列（インデックス）を保持・生成する文脈などで利用します*

2. **文字展開とエスケープ解除**:  
選択されたコードポイントを出力し、保護されていた `{{` を `{` に復元します  
なお、適用可能なコードポイントが存在しない場合は表示システムが規定する豆腐文字（例: `U+25A1`「□」）を出力します

#### 入力と出力結果の例

* **実装優先モード**:
    * 入力: `奈良県葛城市の{MJ022335 b=U+845B v=U+845B U+E0102 set=mj}です`
    * 出力: `奈良県葛城市の葛󠄂です`（※`U+845B` + `U+E0102` の IVS 文字へ展開）


* **基本優先モード**:
    * 入力: `奈良県葛城市の{MJ022335 b=U+845B v=U+845B U+E0102 set=mj}です`
    * 出力: `奈良県葛城市の葛です`（※基本文字 `U+845B` のみへ展開）


### 4.3 簡略化処理（Simplification Process）

付加された属性を取り除き、基本表現である基本タグ（`{<glyph-name>}` 形式）へ復元・還元する処理です。

1. **属性の削除**: タグ構文から `b`, `v`, `set` の属性記述を除去し、`<glyph-name>` のみを残します
2. **結果出力**: 抽象化された基本タグを出力します

#### 入力と出力結果の例

* **属性除去**:
    * 入力: `タグ: {MJ022335 b=U+845B v=U+845B U+E0102 set=mj}`
    * 出力: `タグ: {MJ022335}`


* **エスケープ保持**:
    * 入力: `{{MJ022335}={MJ022335 b=U+845B v=U+845B U+E0102 set=mj}`
    * 出力: `{{MJ022335}={MJ022335}`



## Annex（補足資料）

### Annex A — Unicode IVS / IVD との整合性（国際標準）

* IVS（Ideographic Variation Sequence）は ISO/IEC 10646 に規定されています
* IVD（Ideographic Variation Database）は Unicode Consortium が管理する異体字データベースです
* Moji_Joho IVD Collection は、日本の文字情報基盤（MJ文字）に基づき Unicode IVD に登録された公式コレクションです
* Glyph Tag の `b`/`v` 二層モデルは、IVD の基本文字＋異体字選択符号モデルと完全に整合します

### Annex B — MJ文字（Moji_Joho IVD）の定義例

MJ文字は Unicode に符号化済みであり、必要に応じて IVS を保持します。

```text
MJ022335: { b: "U+845B", v: "U+845B U+E0102" }   // 奈良県葛城市の「葛」
MJ022336: { b: "U+845B", v: "U+845B U+E0103" }   // 東京都葛飾区の「葛」
```

### Annex C — 行政事務標準文字（MJ＋GJ）の構成

行政事務標準文字は以下の要素で構成されます。

* **MJ文字**: Unicode 符号化済み文字
* **GJ文字**: Unicode 未符号化文字（PUP にて対応）

### Annex D — GJ文字の表現手法（暫定私用面）

Unicode 未符号化である GJ文字は、Glyph Tag において以下の属性構造で表現します。

* **`b`（GJ代替文字 / fallback glyph）**: 意味的・字形的に近い既存の UCS 文字または縮退代替文字を設定
* **`v`（GJ暫定私用コード / PUP）**: 実装基盤における私用面コードポイント（例: `U+100ABC`）を設定

### Annex E — フォント実装と参照情報

MJ文字およびGJ文字を実装した代表的なフォント環境と参照URLは以下の通りです。

* **IPAMJ明朝**: MJ文字の UCS/IVS 実装フォント  
[https://moji.or.jp/mojikiban/font/](https://moji.or.jp/mojikiban/font/)
* **DWPI明朝**: 行政事務標準文字（MJ＋GJ）実装フォント  
[https://www.digitalwidearea.org/dwpi_mincho](https://www.digitalwidearea.org/dwpi_mincho)
* **DWPIex明朝**: 変体かな濁点等の IVS 割り当て実装フォント  
[https://www.digitalwidearea.org/dwpi_mincho](https://www.digitalwidearea.org/dwpi_mincho)

### Annex F — 文字体系の保全と統廃合（文字のライフサイクル管理）

MJ文字やGJ文字を含む公的・民間の文字規格では、運用の過程で実施される「再同定作業」や「規格改訂」に伴い、文字（文字図形識別名）の統合、廃止、符号の割り当て変更、または別の文字識別名への付け替えが発生することがあります。

* **文字の統廃合への影響軽減**: Glyph Tag では、文字表現の実体を文字コードではなく抽象的な `<glyph-name>` として保持します  
このため、将来的な再同定によって複数の識別名が単一の文字へ統合された場合や、新しいコードポイントへ再割り当て（標準化）された場合でも、正規化処理時の文字図形表（マッピングテーブル）を更新するだけで、既存のタグデータを書き換えることなく新たな実装環境へ移行できます
* **縮退表示・代替表現の保証**: 同定の見直しに伴い一時的に実装コード（`v`）が変更または無効化された場合であっても、保持されている基本文字コード（`b`）を参照することで豆腐文字の発生を防ぎ、最低限の文字情報（縮退表示）を維持します

### Annex G — 既存・固有文字体系との連携モデル

Glyph Tag は既存システム等の固有文字体系との親和性を確保します。

* 固有字形 ➔ 文字図形識別名（MJ文字図形名、GJ文字図形名、独自字形識別名等）へ割り当て
* 字形データ ➔ `b`（標準文字コード/縮退代替コード）および `v`（実装コード/私用面コード等）にマッピング
* フォント環境が変更・移動した場合でも、抽象タグとして字形識別情報および縮退表示用情報を保持可能
