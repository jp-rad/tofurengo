[pytest]
# PYTHONPATH settings here apply only to pytest.
# VS Code and Pylance do NOT read this file, so the same paths must also be
# added to pythonpath.env (for the VS Code runtime) and settings.json
# (for IntelliSense). pytest.ini affects pytest execution only.
pythonpath =
    tofurengo/src
    glyph/template_v0_1_0/src
    glyph/mj_v6_02_201/src
    glyph/mj_v6_02_201_onka/src
    glyph/mj_plus_v4_10/src
    glyph/mj_plusx_v1_20/src

testpaths =
    tofurengo/tests
